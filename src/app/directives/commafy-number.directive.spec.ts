import { CommafyNumberDirective } from './commafy-number.directive';

describe('CommafyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new CommafyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
